{
  "_comment": [
    "ssh-mcp MCP server config example.",
    "Copy the 'ssh-mcp' block below into your MCP client's config file.",
    "Replace /path/to/ssh-mcp with this project's absolute path.",
    "",
    "Launch rules - these matter for every stdio MCP client:",
    "1) Do NOT add a 'cwd' field. Some clients drop it when spawning the stdio",
    "   server, so a launch string that resolves the project via the working",
    "   directory fails with 'ModuleNotFoundError' -> 'MCP error -32000:",
    "   Connection closed'. Name an ABSOLUTE interpreter path instead.",
    "2) Do NOT launch through 'uv run'. It re-checks/syncs the project",
    "   environment on every startup: pointless overhead and one more failure",
    "   point on a long-lived stdio path. 'uv' is only for building the venv",
    "   once, via 'uv sync'.",
    "",
    "Connection details are NOT configured here. Every tool call carries its",
    "own 'host' + 'user' and authentication material (password or",
    "ssh_key_filepath). The MCP server is stateless: it does not read or write",
    "credential files, aliases, or connection profiles. Environment variables",
    "only control server behavior such as logging, HTTP auth, and host-key policy."
  ],
  "mcpServers": {
    "ssh-mcp": {
      "command": "/path/to/ssh-mcp/.venv/bin/python",
      "args": [
        "-m",
        "ssh_mcp.mcp_server"
      ],
      "env": {
        "SSH_LOG_LEVEL": "INFO"
      },
      "disabled": false
    }
  }
}
